# Clench Wallet 0.3.24 Release Gate

This file records acceptance evidence for v0.3.24. Do not create the production
tag or publish a GitHub/F-Droid release while a required gate is `FAILED`,
`BLOCKED`, or `PENDING`.

## Candidate

- Version name: `0.3.24`
- Version code: `324`
- Source branch: `codex/security-hardening-v0.3.24`
- Intended release source: protected `master` after reviewed pull-request merge
- Classification: `SECURITY RELEASE CANDIDATE — NOT YET AUTHORIZED TO SHIP`
- Signing rule: no local release-key use; the protected signed-release workflow
  remains the only authorized APK-signing path
- Physical-test rule: automated evidence does not convert any `NOT RUN` row in
  `physical-hardware-gates-v0.3.24.md` into a pass

## Automated verification

| Gate | Status | Required evidence |
| --- | --- | --- |
| Focused security regression tests | PENDING | Entropy, PSBT/raw sighash, canonical merge, secure-window, generation-bound authentication, process-wide native-operation drain/quarantine, PIN throttle, Electrum/Tor, TAPSIGNER, backup, NFC, QR/UR, Keystore/storage, and release-trust suites |
| Full debug unit suite | PENDING | Complete suite with zero failures, errors, or unexpected skips |
| Extended hostile-input matrix | PENDING | Deterministic 5,000-case lane plus all targeted hostile fixtures |
| Android lint | PENDING | Zero error/fatal findings |
| Debug and strict unsigned release assembly | PENDING | Package `net.clench.wallet`, version 0.3.24 / 324, unsigned before isolated signing |
| Merged release manifest | PENDING | Expected minimal permissions/features and no debug, cleartext, Bluetooth, USB-signer, or exported unintended surface |
| Release-control and tool self-tests | PENDING | Pinned tag signer, exact protected-master source, minimal isolated signer, no source checkout with secrets, signing-material destruction, checksum and hostile self-tests |
| Exact SBOM and OSV dependency gate | PENDING | Deterministic CycloneDX graph, fail-closed OSV query, zero unresolved findings or only reviewed unexpired exceptions |
| Deterministic provenance | PENDING | All build/control inputs resolved and hashed |
| Independent unsigned APK reproduction | PENDING | Separate clean checkout reproduces the exact unsigned APK payload |
| Hosted Android CI and CodeQL | PENDING | Required on the final PR head and protected-master merge commit |

## Physical hardware and distribution

| Gate | Status | Evidence / blocker |
| --- | --- | --- |
| Physical hardware matrix | NOT RUN | See `physical-hardware-gates-v0.3.24.md`; no publication authorization is implied |
| Signed annotated `v0.3.24` tag | BLOCKED | Requires explicit maintainer authorization after reviewed merge and all mandatory automated gates |
| Protected GitHub release | BLOCKED | Requires an authorized pinned-key tag and a fully passing protected release workflow |
| F-Droid metadata | BLOCKED | Only after a verified GitHub release; preserve established signer and signed-tag updater |
| clench.net | BLOCKED | Update/verify only after the release is public |

## Residual operational controls

- A separate GitHub identity should approve the release-signing environment;
  the current sole maintainer can also administer the repository.
- Move the production APK key to an offline or hardware-backed signing service
  when practical. Repository changes cannot create that independent custody.
- The embedded BDK Rust dependency graph is not represented by the Maven SBOM;
  audit its pinned upstream Cargo lock separately until upstream publishes
  complete nested-component evidence.
- Physical Keystore security level, biometric invalidation, screenshot/Recents
  behavior, NFC interruption/relay resistance, and hardware-wallet behavior
  require instrumentation or real-device evidence.

## Ship decision

`DO NOT SHIP` — this is a release candidate. A separate explicit publication
authorization is required after all mandatory gates are green.
