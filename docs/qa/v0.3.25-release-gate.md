# Clench Wallet 0.3.25 Release Gate

This file records acceptance evidence for v0.3.25. Publication remains
authorized only through the protected release workflow after every automated
gate passes on exact protected `master`.

## Candidate

- Version name: `0.3.25`
- Version code: `325`
- Security-hardening merge: `ce66a6942319cd8a11ec89931fffa67db646c3b1`
- v0.3.24 authorization source: `53100536552eec07ab131188929f41fb1464a49a`
- Classification: `SECURITY RELEASE — MAINTAINER AUTHORIZED TO SHIP`
- Signing rule: only the protected isolated-signing workflow may use the APK
  release key
- Physical-test rule: the maintainer-reported checks authorize publication but
  do not create device-specific pass records without row evidence

## Fail-closed v0.3.24 result

Protected run
[`30960906412`](https://github.com/clenchwallet/clench-wallet/actions/runs/30960906412)
verified the pinned source tag, built without secrets, signed only the approved
APK, destroyed temporary key material, confirmed signer continuity, and
created provenance/SBOM attestations. The independent verifier then rejected
an unexpected `clench-0.3.24-release.apk.idsig` file. Reconstruction and
publication were skipped, and no v0.3.24 GitHub release exists.

## Required v0.3.25 evidence

| Gate | Status | Evidence |
| --- | --- | --- |
| Explicit v4 sidecar disable | PASS | The isolated pinned `apksigner` command requires `--v4-signing-enabled false` |
| Post-sign sidecar assertion | PASS | Signing fails unless `clench-0.3.25-release.apk.idsig` is absent |
| Release-control regression | PASS | Repository controls reject a workflow missing either sidecar defense |
| Security regression suite | PASS | v0.3.24 hardening remains covered; no application-security behavior was removed |
| Full unit, hostile-input, lint, SBOM, OSV, and unsigned-build gates | PASS / REVALIDATE ON PROTECTED MASTER | Must pass locally, on the PR head, and again after merge |
| Independent APK reconstruction and evidence bundle | WORKFLOW-GATED | Must pass in the protected release run before publication |
| Signed annotated `v0.3.25` tag | PINNED-KEY REQUIRED | Create only after exact-master Android CI and CodeQL pass |
| Physical hardware matrix | AUTHORIZED DEFERRAL | Maintainer reported checks; individual rows remain `NOT RUN` without auditable row evidence |

## Ship decision

`AUTHORIZED TO SHIP` — publish v0.3.25 only from a new pinned-key signed tag
on the exact protected-master commit containing this correction. Do not move
or delete the already-pushed v0.3.24 tag, do not rerun its deterministically
failing workflow, and do not publish either version manually.
