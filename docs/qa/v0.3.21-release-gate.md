# Clench Wallet 0.3.21 Release Gate

This file records acceptance evidence for the v0.3.21 candidate. A release must not be published while any required gate is `BLOCKED`, `FAILED`, or `PENDING`.

## Candidate

- Version name: `0.3.21`
- Version code: `321`
- Source branch: `fix/multisig-workflow-v0.3.21`
- Source commit: `PENDING` until the protected PR is merged
- Release decision: `PENDING`
- Maintainer authorization: `PENDING` after hosted checks and final review

## Automated and emulator gates

| Gate | Status | Evidence |
| --- | --- | --- |
| Unit tests | PASS | 207 debug unit tests passed locally, including new account-key normalization and multisig witness-size regression coverage. |
| Lint | PASS | `lintDebug` completed without errors. |
| Debug build | PASS | `assembleDebug` completed successfully. |
| Fresh testnet onboarding | PASS | On Android 16 / API 36 without an OS credential or enrolled biometric, onboarding displayed the unavailable-authentication warning and persisted both seed-view and send authentication gates as disabled. |
| Fresh phone-signer multisig creation | PASS | The emulator created a new 2-of-2 testnet wallet with one Clench phone signer and one independent BIP-48 public cosigner. BDK accepted clean external `/0/*` and change `/1/*` descriptors. |
| Funded testnet PSBT review and phone signature | PASS | A preserved wallet with a confirmed 10,000-sat Testnet3 UTXO built a 1,000-sat self-send PSBT. Review displayed `193 sats (estimated final: 1.00 sat/vB, 193 vB)`, and the Clench phone signer produced a signed PSBT without the former authentication dead end. |
| Incomplete multisig broadcast refusal | PASS | The one-signature 2-of-2 PSBT remained incomplete; `Broadcast If Complete` refused finalization. No testnet transaction was broadcast. |
| Strict release-like build | PASS | A temporary clean worktree completed strict dependency verification, 207 unit tests, `lintDebug`, R8 minification, and unsigned `assembleRelease`. `aapt` reports package `net.clench.wallet`, version 0.3.21 / code 321; unsigned APK SHA-256: `4cdae44842df578e4c7547407ec9ff103ef222a8da50c3534b58ba00697fc4ce`. |
| Hosted Android CI and CodeQL | PENDING | Required protected-branch checks start after the PR is pushed. |

## Store and release gates

| Gate | Status | Evidence / blocker |
| --- | --- | --- |
| F-Droid localized metadata | PASS | The source short description is 58 bytes and contains the exact phrase “Bitcoin wallet”; the full description retains Bitcoin-only positioning. |
| F-Droid v0.3.20 publication | EXTERNAL | Official `fdroiddata` includes 0.3.20 / 320, while the public repository page still lists 0.3.19. This does not block the v0.3.21 source candidate. |
| Physical hardware signers | DEFERRED | No SATSCARD, TAPSIGNER, Coldcard, SeedSigner, Keystone, Passport, or Jade acceptance is claimed for this change. |
| Signed annotated `v0.3.21` tag | PENDING | Create only after merge, final release authorization, and a release gate with no required pending checks. |
| Protected release workflow | PENDING | The signed build, signer continuity, checksums, provenance, package/version verification, and publication run only from the verified tag. |

## Ship decision

`PENDING` — do not tag or publish v0.3.21 until the protected PR checks pass, the strict final-source validation is recorded, and the maintainer explicitly authorizes the signed release.
