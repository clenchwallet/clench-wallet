# 0.3.34 maintenance release gate

Production correction: PR #102, merged as
`24e24d532399cd3bed74650dea9ade1abe7ebe04`.
Independent source/runtime-evidence review: PASS at
`081961da99e19eb109bd11ba21b7a748db8273b7`, review 5311031526.
All candidate hosted checks passed, including Android instrumentation.

The final-source preparation adds only 0.3.34/334 version metadata, release/gate
notes and the corresponding exact native applicability binding. Production Kotlin,
resources, dependencies, native inputs and the completed #101 regression are
unchanged. The reviewed 14-stage release-mode runtime evidence is retained;
no new emulator or hardware run is required for this metadata-only delta.
See [startup reentry](../verification/startup-reentry.md).

Before signing, the unchanged protected release workflow must verify the trusted
annotated tag against current protected master, test/lint/build final source, run
strict dependency/SBOM/native advisory gates and prove blind raw-byte reproducibility.
Its source-free protected signer and separate post-sign verification must pass.
Publication must create a new v0.3.34 release; existing releases must not be replaced.
After publication, the public-release workflow independently rebuilds and verifies
the downloaded bundle before website version update and actual deployment.
Execution status and URLs are retained in the release checkpoint/workflow receipts,
not asserted in advance by this source document.

No new claim is made for broader audit gaps. Existing native dispositions retain
all six exact entries and original 2026-10-16 expiry. #101/#99 stay closed.
