# Clench Wallet 0.3.22 Release Gate

This file records acceptance evidence for the v0.3.22 candidate. Do not create the production tag or publish a GitHub/F-Droid release while a required gate is `FAILED`, `BLOCKED`, or `PENDING`.

## Candidate

- Version name: `0.3.22`
- Version code: `322`
- Source branch: `codex/high-assurance-rc-0.3.22`
- Candidate commit: the final pull-request head containing this authorization;
  record its exact SHA and required hosted checks in the PR evidence before
  merge. The signed tag must resolve to the reviewed `master` merge commit.
- Intended release source: protected `master` after reviewed pull-request merge
- Classification: `PRODUCTION RELEASE AUTHORIZED WITH EXPLICIT PHYSICAL-HARDWARE DEFERRAL`
- Signing rule: no local signing-key use for candidate verification; the protected release workflow remains the only authorized production-signing path
- Maintainer ship authorization: received 2026-07-27 UTC. This authorizes the
  documented physical-hardware deferral and publication only through the
  existing fail-closed signed-release workflow; it does not convert any
  `NOT RUN` physical test into a pass.

## Automated verification

| Gate | Status | Evidence |
| --- | --- | --- |
| Hostile PSBT / QR / NFC / Coinkite / multisig / storage / fee / interruption / recovery suites | PASS (LOCAL SOURCE) | Clean deterministic matrix passed at `CLENCH_FUZZ_CASES=1024`; the separate 5,000-case hostile lane also passed. Fixed seeds and failing case indexes are replayable. Hosted confirmation remains required on the final PR commit. |
| Full debug unit suite | PASS (LOCAL SOURCE) | 253 tests in 42 suites; 0 failed, 0 errors, 0 skipped. Report: `app/build/reports/tests/testDebugUnitTest/index.html`. |
| Android lint | PASS (LOCAL SOURCE) | `lint` completed with 0 errors (73 warnings and 2 hints remain non-blocking); report: `app/build/reports/lint-results-debug.html`. |
| Debug APK assembly | PASS (LOCAL SOURCE) | `net.clench.wallet.debug`, version 0.3.22 / 322, 62,068,766 bytes, SHA-256 `28114223bdc9b244babce5d0107a9e5baf64d1f70a9384dd6619890b194a7c83`. Rebuild after the final evidence commit. |
| Strict unsigned release assembly | PASS (EXACT SOURCE SNAPSHOT) | Two standalone clean clones of `f9d6b8a600b2e4679fd22d5960f55964ada44f5e`, neither containing signing material, produced byte-identical 36,251,133-byte unsigned APKs. Clone B used a separate cold `GRADLE_USER_HOME`. Both APKs are `net.clench.wallet` 0.3.22 / 322 with SHA-256 `013a0af3424cc59816f9b435d75a1cf1cccb7f69b68da28f8f31484f2966a796`. Repeat after this evidence-only gate commit and record the final PR-head digest in pull-request evidence. |
| Deterministic SBOM | PASS (EXACT SOURCE SNAPSHOT) | Independent generation in both source clones produced byte-identical canonical CycloneDX 1.6 documents. Each validator inventoried 189 locked release-runtime components with Gradle-verified artifact hashes. SHA-256: `2c2c34910c1bb12d845f45a17ef2eefa23ce2d61ba8225e5b0dbbdb6bb820588`. Repeat at final PR head. |
| Deterministic provenance statement | PASS (EXACT SOURCE SNAPSHOT) | Independent generation in both source clones over identically named APK/SBOM subjects produced byte-identical canonical in-toto/SLSA statements with 20 exact resolved inputs. SHA-256: `a15e42f0f9635c98fdf66deb5d3b8750cd790e12035796810d67cca1e8e9fc00`. Repeat at final PR head. |
| Release-key isolation policy | PASS (EXACT SOURCE SNAPSHOT) | Static policy at `f9d6b8a600b2e4679fd22d5960f55964ada44f5e` confirms signing secrets are confined to the protected signing job and destroyed before attest/upload. Neither independent clone contained signing material. Repeat at final PR head and hosted CI. |
| Live release governance | RESIDUAL / AUTHORIZED | Technical isolation passes; the environment permits the sole reviewer to self-review and permits administrator bypass. The maintainer explicitly authorized shipment on 2026-07-27 UTC with the physical-hardware boundary retained. See `docs/security/release-key-isolation-v0.3.22.md`. |
| Independent APK payload verifier | PASS (ESTABLISHED RELEASE) | An exact standalone clone of signed tag `v0.3.21` (`b63778ed15b5b3a35a72d270b7ed7c7448aeac8d`) independently rebuilt unsigned APK SHA-256 `06859cedc9db1448ce8a268fdb3e29239a55b9373b8c9027468150c329506837`. All 523 payload entries matched published APK SHA-256 `fa82120f33b58d966a3c2f4a36c74b0d1569c16a82b2d7d74cad7eb5b59938a9` exactly; only paired v1 signature records were excluded. Report SHA-256: `316028e6d1e72fac03ee8c4629d8bd7da1082808439a7cd342c7599e56b81529`. The v0.3.22 protected workflow must repeat this against its signed candidate. |
| Hosted Android CI and CodeQL | PASS ON VERIFIED IMPLEMENTATION HEAD / REQUIRED ON FINAL GOVERNANCE HEAD AND MERGE | Android CI and CodeQL passed on implementation head `5a004323a7ed19e8cd013dc6e817f66d68058417`. The authorization-only final PR head must pass the same required checks before merge, and the protected `master` merge commit must pass before tagging. |

## Transaction lifecycle

| Gate | Status | Evidence / safe boundary |
| --- | --- | --- |
| Fresh Testnet3 receive and confirmation | PASS (LIVE SOFTWARE) | Disposable 10,400-sat native-SegWit funding confirmed at height 5,083,198. See `testnet-lifecycle-v0.3.22.md`. |
| Build and immutable review | PASS (LIVE SOFTWARE) | Exact destination, amount, fee, fee rate, vsize, inputs, outputs, and txid were reviewed before each local signature/broadcast. |
| Sign and finalize | PASS (LIVE SOFTWARE) | Disposable phone signer completed the intended self-spend, RBF replacement, and faucet return; no production material was used. |
| Broadcast and confirmation | PASS (LIVE SOFTWARE) | The self-spend replacement confirmed at height 5,083,203; the final faucet return confirmed at height 5,083,214. |
| RBF fee bump | PASS (LIVE SOFTWARE) | Replacement `ad032ba9…dd9d6e` preserved the destination, required a fresh review/high-fee acknowledgment, and confirmed. |
| Cancel-by-RBF | NOT COMPLETED (TIMING GATE) | Three valid low-fee targets confirmed before the software-TCG emulator could complete replacement review. No safety gate was bypassed. Deterministic cancel/review coverage is not claimed as a live pass. |
| CPFP | NOT COMPLETED (TIMING GATE) | No unconfirmed owned parent remained available. Deterministic selection/fee/review coverage is not claimed as a live pass. |
| Multisig incomplete-signature refusal | PASS (AUTOMATED) / PHYSICAL NOT RUN | Deterministic incomplete-signature and interrupted-session suites refuse broadcast. Physical independent-signer confirmation remains in the hardware matrix. |

The live run used an official Android API 36 AVD under software TCG. It proves
the Android/software and Testnet3 paths recorded above, not physical hardware.

## Physical hardware and distribution

| Gate | Status | Evidence / blocker |
| --- | --- | --- |
| Physical hardware matrix | AUTHORIZED DEFERRAL / NOT RUN | Maintainer authorized publication on 2026-07-27 UTC with this explicit deferral. See `physical-hardware-gates-v0.3.22.md`; no simulator result is a physical pass, and the public release notes retain this boundary. |
| Signed annotated `v0.3.22` tag | AUTHORIZED AFTER REVIEWED MERGE | Create only from the reviewed `master` merge commit, using the existing GitHub-registered maintainer tag-signing key. A new, unsigned, lightweight, or unverified tag remains prohibited. |
| Protected GitHub release | AUTHORIZED / WORKFLOW-GATED | Publication remains conditional on the existing workflow verifying the signed tag, protected signing environment, no-secrets independent rebuild, signer continuity, exact SBOM attestation, provenance, and exact signed-vs-unsigned APK payload comparison. |
| F-Droid metadata | READY AFTER RELEASE | F-Droid currently suggests reproducible 0.3.21 / 321; `fdroiddata` uses tag-based auto-update and the established signer. Do not pre-publish 0.3.22 metadata before its signed GitHub release. |
| clench.net | DEFERRED | Do not change public version or reproducibility claims until the production release is live and verified. |

## Ship decision

`SHIP AUTHORIZED WITH EXPLICIT PHYSICAL-HARDWARE DEFERRAL` — the source and
verification laboratory have exact local evidence, and the maintainer
authorized publication on 2026-07-27 UTC without representing any physical
hardware row as passed. The final authorization-only PR head and protected
`master` merge must retain green hosted checks. Publication must then occur
only through the existing signed-release workflow, which must verify the
registered signed tag, protected signer, attestations, deterministic evidence,
and independent APK payload comparison before creating the GitHub release.
Any failure in those checks cancels publication.
